services:
  - type: web
    name: agriqueue-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: PORT
        value: 5000
      - key: JWT_SECRET
        value: agriqueue_super_secret_jwt_key_2026
